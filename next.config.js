/** @type {import('next').NextConfig} */
const nextConfig = {
    output: 'export', // Включает static export
    trailingSlash: true, // Добавляет / в конце ссылок (например, /about/)
  };
  
  module.exports = nextConfig;
  